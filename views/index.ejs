<% if (user && user.id && user.login) { %>
  <%- include('layout/header') %>
    <div class="main">
      <div class="container">
        <div class="dialogs-wrapper">
          <h2>Chat</h2>
          <div class="dialogs">
            <!-- Данные загружаются через GraphQL -->
            <p>Загрузка...</p>
          </div>
        </div>
      </div>
    </div>
    <%- include('layout/footer') %>
      <% } %>